import 'package:flutter/material.dart';

class AppColors {
  static const gradientStart = Color.fromARGB(255, 240, 234, 180); // Soft gold
  static const gradientMiddle = Color(0xFFE0F2FE); // Light blue
  static const gradientEnd = Color(0xFFF0FDF4); // Mint green
  
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const gray50 = Color(0xFFF9FAFB);
}

class AppConstants {
  static const double pageWidth = 375.0;
  static const double pageHeight = 812.0;
  static const double borderRadius = 12.0;
  static const double cardPadding = 16.0;
} 